<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" type="text/css" href="../css/style.css">
    <script src="../js/app.js"></script>
    <title>Exercise Journal</title>
</head>
<body>
    <div class="container-fluid bg-light">
        <!--Navigation -->
        <%- include('layouts/navbar.ejs') -%>
        <h1>Journal</h1><hr><br>
        <h5>You can see your journal entries posted below.</h5>
        <br>
        <br>
        <h6>Use our filter to track previous months so you can see all the great progress you've made!</h6>
        <form id="filter" action="/journal/filter" method="get">
            <label for="start_date">Start Date:</label>
            <input type="date" id="start_date" name="start_date" required>

            <label for="end_date">End Date:</label>
            <input type="date" id="end_date" name="end_date" required>

            <button type="submit" class=" btn btn-primary">Filter</button>
        </form>
        <br>
        <table class="table table-hover" id="saved_ex">
            <thead class="table-primary">
                <tr>
                    <th>Date</th>
                    <th>Body Part</th>
                    <th>Exercise</th>
                    <th>Weight/ Time</th>
                    <th>Sets</th>
                    <th>Repetitions</th>
                    <th>Remove Post</th>
                </tr>
            </thead>
            <tbody>
                <% all_entries.forEach(entry => { %>
                    <tr>
                        <td><%= entry.date %></td>
                        <td><%= entry.body_part %></td>
                        <td><%= entry.exercise %></td>
                        <td><%= entry.weight %></td>
                        <td><%= entry.sets %></td>
                        <td><%= entry.reps %></td>
                        <td>
                            <form id="remove" action="/posts/remove" method="post">
                                <input type="hidden" name="id" value="<%= entry.id %>">
                                <button type="submit" class="btn btn-warning btn-sm">Remove</button>
                            </form>
                        </td>
                    </tr>
                <% }) %>
            </tbody>
        </table>
<!--        <div id="heatmap"></div>-->
<!--        <script src="https://cdn.plot.ly/plotly-latest.min.js"></script>-->

<!--        <button type="submit" class="btn btn-warning btn-sm">Archive</button>-->
        <br>
        <br>
        <br>
        <br>
        <!--Footer -->
        <%- include('layouts/footer.ejs') -%>
    </div>
</body>
</html>